<article class="execution box {{ sch.status }}">
    <div class="row">
        <div class="col-md-10">
            <h5 class="status-text {{ sch.status }}">{{ sch.title }}</h5>
            {% if schUrl %}
            <h3>
                <a href="{{ schUrl }}">{{ schTitle }}</a>
            </h3>
            {% endif %}
        </div>
        <div class="col-md-2 text-right">
            <span class="status-text {{ sch.status }}">{{sch.description}}</span>
        </div>
    </div>

    <div class="sub">
        {% for sub in sch.sub | reverse %}
            <div class="row">
                <div class="col-md-8">
                    {% if sub.source %}
                        <a href="{{ sub.source }}" target="blank" >
                            <span class="padding-right">{{ sub.title }}</span>
                        </a>
                    {% else %}
                        <span class="padding-right">{{ sub.title }}</span>
                    {% endif %}
                    {% if sub.date %}
                        <span class="muted">{{ sub.date | date}}</span>
                    {% endif %}
                </div>

                <div class="col-md-4 text-right">
                    <span class="status-text {{ sub.status }}">{{ sub.description }}</span>
                </div>

            </div>
        {% endfor %}
    </div>

</article>